export interface Alien {
    id: string;
    articleName: string;
    name: string;
}

export interface AlienInformation {
    id: string;
    name: string;
    species: string;
    homePlanet: string; 
    body: string;
}
